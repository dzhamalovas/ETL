
graph TD

    %% ===== External Layer =====
    subgraph External Sources
        API[Weather API]
    end

    %% ===== Infrastructure Layer =====
    subgraph Docker Infrastructure

        PG[(PostgreSQL<br/>Airflow Metadata)]

        subgraph Airflow
            INIT[Init Container]
            SCH[Scheduler]
            WEB[Webserver<br/>:8080]
        end

        subgraph Data Layer
            VOL[(Shared Volume<br/>/opt/airflow/data)]
        end

        subgraph Visualization Layer
            STR[Streamlit App<br/>:8501]
        end

    end

    %% ===== User Layer =====
    subgraph User
        BROWSER((Browser))
    end

    %% ===== Data Flow =====
    API -->|JSON Forecast| SCH
    SCH -->|ETL → CSV| VOL
    SCH <-->|Metadata| PG
    WEB <-->|Metadata| PG

    STR -->|Reads Data| VOL

    %% ===== User Interaction =====
    BROWSER -->|Airflow UI| WEB
    BROWSER -->|Dashboard| STR
